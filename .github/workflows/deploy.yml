name: Deploy to Railway

on:
  push:
    branches: [main]

jobs:
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore

      - name: Run unit tests
        run: |
          for project in AuthService ProfileService JobService ApplicationService AIService NotificationService; do
            dotnet test "tests/${project}.UnitTests" --no-build --logger "console;verbosity=normal"
          done

      - name: Run integration tests
        run: |
          for project in JobService ApplicationService; do
            dotnet test "tests/${project}.IntegrationTests" --no-build --logger "console;verbosity=normal"
          done

  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/frontend/web
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: src/frontend/web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit & component tests
        run: npx vitest run

  deploy:
    name: Deploy Services
    needs: [backend-tests, frontend-tests]
    runs-on: ubuntu-latest
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm i -g @railway/cli

      - name: Deploy Auth Service
        run: railway up --service auth --detach

      - name: Deploy Profile Service
        run: railway up --service profile --detach

      - name: Deploy Job Service
        run: railway up --service job --detach

      - name: Deploy Application Service
        run: railway up --service application --detach

      - name: Deploy AI Service
        run: railway up --service ai --detach

      - name: Deploy Notification Service
        run: railway up --service notification --detach

      - name: Deploy API Gateway
        run: railway up --service gateway --detach

      - name: Deploy Frontend
        run: railway up --service frontend --detach

  sentry-release:
    name: Create Sentry Release
    needs: [deploy]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create Sentry release
        uses: getsentry/action-release@v3
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: roshan-chapagain
        with:
          projects: matchura-services matchura-frontend
          environment: production
